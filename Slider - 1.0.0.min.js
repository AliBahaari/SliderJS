'use strict';

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function Slider(objectVar) {

  var buttonWidth = objectVar.buttonWidth;
  var buttonHeight = objectVar.buttonHeight;
  var backGround = objectVar.backGround;
  var borderRadius = objectVar.borderRadius;
  var borderWidth = objectVar.borderWidth;
  var borderStyle = objectVar.borderStyle;
  var borderColor = objectVar.borderColor;

  var targetElements = document.querySelectorAll('.SliderJS div');
  document.body.style.overflow = 'hidden';

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = targetElements[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var childElement = _step.value;

      childElement.style.cssText = '\n      width: 100%;\n      height: 100vh;\n    ';
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator.return) {
        _iterator.return();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  var sliderButtons = document.createElement('ul');
  sliderButtons.className = 'SliderButtons';
  sliderButtons.style.cssText = '\n    position: fixed;\n    top: 50%;\n    left: 15px;\n    transform: translateY(-50%);\n    list-style-type: none;\n  ';

  for (var i = 0; i < targetElements.length; i++) {
    var sliderButton = document.createElement('li');
    sliderButton.style.cssText = '\n      width: ' + buttonWidth + ';\n      height: ' + buttonHeight + ';\n      background: ' + backGround + ';\n      border-radius: ' + borderRadius + ';\n      border-width: ' + borderWidth + ';\n      border-style: ' + borderStyle + ';\n      border-color: ' + borderColor + ';\n      margin: 5px 0;\n      cursor: pointer;\n    ';
    sliderButtons.appendChild(sliderButton);
  }
  document.body.appendChild(sliderButtons);

  document.querySelectorAll('.SliderButtons li').forEach(function (selectedButton) {
    selectedButton.onclick = function () {
      var buttonIndex = [].concat(_toConsumableArray(selectedButton.parentElement.children)).indexOf(selectedButton);

      document.querySelectorAll('.SliderJS div').forEach(function (selectedFrame) {
        var frameIndex = [].concat(_toConsumableArray(selectedFrame.parentElement.children)).indexOf(selectedFrame);

        if (buttonIndex == frameIndex) {
          window.scrollTo({
            top: selectedFrame.offsetTop,
            left: 0,
            behavior: 'smooth'
          });
        }
      });
    };
  });
}